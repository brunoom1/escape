/**
 * @module ThemeDefault
 * @version 1.0.0
 * @description Tema padrão da marca Escape Room. Escuro com acentos amarelos.
 * Este é o fallback — se nenhum tema for aplicado, os tokens defaults já representam este estilo.
 */

const ThemeDefault = {
  // Usa os tokens padrão sem sobrescrever nada.
  // Existe como referência e para aplicação explícita.
};
